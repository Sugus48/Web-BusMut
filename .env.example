# คัดลอกไฟล์นี้เป็น .env แล้วแก้ค่าให้ตรงกับเครื่อง (XAMPP ค่าเริ่มต้น: user root ไม่มีรหัสผ่าน)
PORT=3000
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=mut_shuttle
SESSION_SECRET=change-me-to-a-long-random-string
